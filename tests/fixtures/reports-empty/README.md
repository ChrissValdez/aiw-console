# Reports empty fixture

A project root with a readable reports index that lists nothing, because it has no reports/ folder.
Frozen so the suite can drive the honestly-empty case without reading a live repository.
