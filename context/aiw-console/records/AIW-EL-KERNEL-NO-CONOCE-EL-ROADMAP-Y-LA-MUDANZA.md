# Record — hilo `aiw` · 2026-08-06 · primera sesión en Cowork (modo CONECTADO)

**Medición fechada.** Todo lo de aquí se midió el 2026-08-06 entre las 07:44 y las 20:40 UTC
desde una cabina Linux con el workspace montado. **No se escribió ningún canónico y no se
tocó la consola en toda la sesión.**

---

## 1. El hallazgo central: el kernel de `aiw` no conoce ningún roadmap

`grep` de `roadmap`, `run_id`, `queue_order`, `depends_on`, `closure_mode` y
`correctness_model` sobre `kernel.mjs` **y** `queue.mjs` → **cero apariciones**. Lo único que
el kernel lee de configuración es `config.json`, por la clave que el objetivo declara en
`# Project` (`kernel.mjs:274`).

`parseObjective` entiende **siete secciones y ninguna es `run_id`**: `# Project`,
`# Objective`, `# Acceptance criteria`, `# Scope`, `# Out of scope`, `# Max rounds`,
`# Verification`.

`queue.mjs` son **69 líneas**: `readdirSync(pending).filter(.md).sort()`. **Ordena nombres de
fichero.** Un fallo no detiene la cola. Archiva a `processed/<ESTADO>-<nombre>.md` con
`git mv` si el objetivo está trackeado. Historia real: **13 procesados — 10 `APPROVED`,
2 `HUMAN_REVIEW`, 1 `ERROR`**.

**Consecuencia:** el orden de ejecución lo fija el nombre que la cabina elige. Con relleno de
tres dígitos el orden alfabético es el `queue_order`; sin relleno, `cql-10` va antes que
`cql-9`.

### El kernel nunca cierra nada

No mergea: deja el trabajo en `aiw/<id>` y devuelve el árbol a la base (`:450-458`), solo con
árbol limpio. Emite `APPROVED`(0) / `BLOCKED`(3) / `HUMAN_REVIEW`(2 y 4). En los dos últimos
escribe `proposed_followup.md`, cuya última línea es *«(Draft generated by the kernel; the
human decides.)»*

**Adjudicación: hoy no existe la distinción unattended / semi-attended en el kernel. Todo es
semi-attended.** Lo que falta no es cómo frenar los semi-atendidos, sino cómo **cerrar** los
desatendidos. Y la pieza que falta es **`#31` «The intake»**, cuya adjudicación de `CONST §4`
lleva abierta desde el 2026-07-28 y solo el operador puede cerrar.

---

## 2. Cuatro correcciones a mediciones anteriores — gana el disco

| # | lo que afirmaba | lo medido |
|---|---|---|
| 1 | «pasa el `status` por `--ignore-cr-at-eol`» | **`git status` NO tiene esa opción** (git 2.34.1). Es de `git diff`. La forma que funciona: **`git -c core.autocrlf=true status --porcelain`** → 0 en `aiw` |
| 2 | «`aiw`: 21 `planned`, 0 `active`» | **25 `completed`, 20 `planned`, 1 `active`.** El 21 era el conteo de VIVOS. El handoff se contradecía con su propia §2 |
| 3 | «md5 del canónico `5c7cf8bd…`» | **`ce7a9f7b0feed3651ddb0bd18f2680e1`** (bytes CRLF) · `a57601a710b591dc0484577393163f9b` normalizado a LF. Vencido desde `6383e51` (2026-08-04) |
| 4 | «`verification` solo admite `npm test` en la práctica; que acepte LaTeX es trabajo de `aiw`» | **Falso.** `execProc` hace `spawn(..., { shell: true })` (`kernel.mjs:100`); es una cadena de shell cualquiera, y `kernel.mjs:278` permite que el objetivo traiga la suya. **Cero trabajo de `aiw`** |

**Trampa nueva y no obvia:** `git diff --ignore-cr-at-eol --name-only` **también miente**
(119 en `aiw`), porque `--name-only` no respeta los flags de whitespace. Solo `--stat` y el
parche los respetan. Una guarda construida sobre `--name-only` engaña igual que la ingenua.

**Y un aviso de método:** re-medir la compuerta `CONST §4` con un patrón sobre
«MECHANISM, INCIDENT PENDING» da **9/12** y es incorrecto. **`#23` lo redacta distinto**
—*«THIS RUN ADDS MECHANISM AND ITS INCIDENT IS PENDING»*—. Leído verbatim, el reparto
**8 libres / 13 detenidos se confirma**. La compuerta se lee verbatim, no por patrón.

---

## 3. Dos capacidades que `D-064` declara mal

| `D-064` | la prueba del 2026-08-06 |
|---|---|
| «`.git` no es escribible» | **SÍ lo es.** `touch aiw/.git/_probe` funcionó; `git commit` solo falló por identidad de autor sin configurar |
| (no lo declara) | **el CLI `claude` está presente**, 2.1.221 |

**Sigue confirmado que la cabina no puede BORRAR** (`rm` → *«Operation not permitted»*).

**Y el error de esta sesión:** la cabina sondeó **dentro** de un repo y dejó
`aiw/objectives/pending/_probe.md`, que `queue.mjs` habría levantado e intentado ejecutar
como objetivo, más `aiw/.git/_probe`. **Los sondeos van a `_scratch\`.** La regla existía y
se rompió.

---

## 4. `#22` — el `007` conserva su trabajo entero

**Medido, no preguntado:** `v3RunRowTags` (`project-console.js:3419`) empuja la etiqueta de
`severity` en `:3433` y **no tiene ningún `tags.push` de `closure_mode`**. `closure_mode`
aparece 8 veces en el fichero, ninguna dentro de esa función. Coordenadas: función `3419`,
llamadas `3449` y `4521`, comentario en `3169`.

Al medirlo, `project-console.js` seguía en **`6ee3277` (2026-08-04)** — el mismo SHA en que se
cedió la ventana. **`aiw-console` no lo había tocado.** *(Al cierre de sesión el fichero ya
está sucio por el motor de lotes: las coordenadas hay que volver a derivarlas.)*

`locks/` **no existe** como directorio (no «vacío»), no hay `logs/007*`, y no hay rama
`aiw/007*`. El objetivo sigue intacto: 7 080 bytes, md5
`fb2aabbe6897c8de4f1a19637ce0ec76`, trackeado.

**El operador decidió dejar `#22` en `active`** pese a que nada corre. Queda registrado que
el canónico afirma un taller que no existe.

---

## 5. `cantu-quizzes-latex` pasa de «no disponible» a blanco viable

El relevo anterior lo descartaba por «0 de 8 binarios de LaTeX». **Corregido:** `latexmk`
**4.88** está instalado en Windows, confirmado por el operador.

**Su roadmap, medido a las 13:24 CST: 42 runs, los 42 clasificados completos.** Corriendo
`aiw-console/tools/classification/classification.mjs` sobre ellos:
**41 `SEMI_ATTENDED` · 1 `ATTENDED`** (`#2`, piloto, `JUDGED_DEFINES` + `CRITICAL`). Solo dos
aristas: `#7` y `#37`, ambas de `#1`.

**Dos bloqueos medidos, ambos de su hilo:**

1. **No tiene `.gitignore` y tiene 24 artefactos de build trackeados** (4 cada uno de `.aux`,
   `.log`, `.out`, `.synctex.gz`, `.pdf`, `.auxlock`). Verificar reescribe ficheros
   versionados → `BLOCKED_SCOPE` **en la ronda 1, con certeza aritmética**.
2. **32 paquetes de TeX exigidos, sin declarar en ninguna parte.** Cinco no están en una
   instalación base: `fontawesome`, `montserrat`, `eulervm`, `newpxmath`, `newpxtext`. Sin
   ellos, `D-012` (`kernel.mjs:328`) aborta con «red baseline». **La cabina no pudo compilar
   por eso** — el fallo fue del entorno de la cabina, no del repo.

**Por eso `queue.mjs` lo corre el operador y no la cabina:** es una medición, no una regla.

**Reparto acordado con el operador:** este hilo registra CQL en `config.json`, escribe un
`.md` de objetivo por run y mide los resultados desde disco; el operador corre `queue.mjs` y
da QA; el hilo de CQL cierra sus runs. **Dispara todo una sola cosa: `O2` verde.**

**Y el trabajo manual de escribir esos 40 ficheros es el incidente con sus cuatro campos que
desbloquea `#31`.**

---

## 6. Estado de mudanza — el operador pasa a una laptop nueva

**Los cinco repos con `ahead=0`: todo empujado.** Lo que no viaja:

- **`aiw` y `aiw-console` no tienen `.gitattributes`.** El commit `5ce887a` documenta que
  esto ya puso rojo el pin de EOL de `roadmap-engine.test.mjs`. En una máquina nueva muerde
  el primer día.
- **`aiw/config.json` tiene rutas absolutas de Windows.** Si cambia usuario o ruta, el kernel
  aborta en `kernel.mjs:286`. El `path` de `sandbox` ya no existe hoy.
- **`_scratch\` (12 ficheros) y `_backups\` (8 + 6) están fuera de todos los repos.**
- **Trabajo sin commitear en dos repos:** `aiw-console` con el motor de lotes a medio hacer
  más `tests/fixtures/batches/` sin trackear, y `cantu-quizzes-latex` con el piloto de
  fracciones en curso.

---

## 7. Lo que queda abierto

1. **La adjudicación de `#31`** — abierta desde el 2026-07-28, solo el operador la cierra.
2. **`#22` en `active` sin taller.** Revertirlo exige palabra del operador: `D-064` no
   concede `active → planned`.
3. **El acuerdo del §5 no se registró en `DECISIONES.md`** porque el fichero tenía trabajo de
   otro hilo sin commitear. Se registra en cuanto el árbol lo permita.
4. **`#34` sigue siendo el desbloqueo más barato del roadmap:** una entrada de papel para el
   criterio de borrado de `#33` abre dos runs.
